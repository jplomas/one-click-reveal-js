{
  "name": "one-click-reveal-js",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve": "parcel serve 'src/**/*.html'",
    "build": "parcel build 'src/**/*.html' --dist-dir ./build"
  },
  "author": "JP Lomas",
  "license": "MIT",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "autoprefixer": "^10.4.2",
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "reveal.js": "^4.2.1"
  },
  "sass": {
    "includePaths": [
      "./node_modules"
    ]
  }
}
